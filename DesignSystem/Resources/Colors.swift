import Foundation
import SwiftUI

public struct Colors {
  static let background = Color("background", bundle: ModuleInfo.bundle)
  static let pureWhite = Color("white", bundle: ModuleInfo.bundle)
  static let pureBlack = Color("black", bundle: ModuleInfo.bundle)

  struct Primary {
    static let Base = Color("primaryBase", bundle: ModuleInfo.bundle)
    static let Dark = Color("primaryDark", bundle: ModuleInfo.bundle)
    static let Light = Color("primaryLight", bundle: ModuleInfo.bundle)
    static let Lighter = Color("primaryLighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("primaryLightest", bundle: ModuleInfo.bundle)
  }

  struct Secondary {
    static let Base = Color("secondaryBase", bundle: ModuleInfo.bundle)
    static let Dark = Color("secondaryDark", bundle: ModuleInfo.bundle)
    static let Light = Color("secondaryLight", bundle: ModuleInfo.bundle)
    static let Lighter = Color("secondaryLighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("secondaryLightest", bundle: ModuleInfo.bundle)
  }

  struct Highlight1 {
    static let Base = Color("highlight1Base", bundle: ModuleInfo.bundle)
    static let Dark = Color("highlight1Dark", bundle: ModuleInfo.bundle)
    static let Light = Color("highlight1Light", bundle: ModuleInfo.bundle)
    static let Lighter = Color("highlight1Lighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("highlight1Lightest", bundle: ModuleInfo.bundle)
  }

  struct Highlight2 {
    static let Base = Color("highlight2Base", bundle: ModuleInfo.bundle)
    static let Dark = Color("highlight2Dark", bundle: ModuleInfo.bundle)
    static let Light = Color("highlight2Light", bundle: ModuleInfo.bundle)
    static let Lighter = Color("highlight2Lighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("highlight2Lightest", bundle: ModuleInfo.bundle)
  }

  struct Highlight3 {
    static let Base = Color("highlight3Base", bundle: ModuleInfo.bundle)
    static let Dark = Color("highlight3Dark", bundle: ModuleInfo.bundle)
    static let Light = Color("highlight3Light", bundle: ModuleInfo.bundle)
    static let Lighter = Color("highlight3Lighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("highlight3Lightest", bundle: ModuleInfo.bundle)
  }

  struct Highlight4 {
    static let Base = Color("highlight4Base", bundle: ModuleInfo.bundle)
    static let Dark = Color("highlight4Dark", bundle: ModuleInfo.bundle)
    static let Light = Color("highlight4Light", bundle: ModuleInfo.bundle)
    static let Lighter = Color("highlight4Lighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("highlight4Lightest", bundle: ModuleInfo.bundle)
  }

  struct Info {
    static let Base = Color("infoBase", bundle: ModuleInfo.bundle)
    static let Dark = Color("infoDark", bundle: ModuleInfo.bundle)
    static let Light = Color("infoLight", bundle: ModuleInfo.bundle)
    static let Lighter = Color("infoLighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("infoLightest", bundle: ModuleInfo.bundle)
  }

  struct Danger {
    static let Base = Color("dangerBase", bundle: ModuleInfo.bundle)
    static let Dark = Color("dangerDark", bundle: ModuleInfo.bundle)
    static let Light = Color("dangerLight", bundle: ModuleInfo.bundle)
    static let Lighter = Color("dangerLighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("dangerLightest", bundle: ModuleInfo.bundle)
  }

  struct Success {
    static let Base = Color("successBase", bundle: ModuleInfo.bundle)
    static let Dark = Color("successDark", bundle: ModuleInfo.bundle)
    static let Light = Color("successLight", bundle: ModuleInfo.bundle)
    static let Lighter = Color("successLighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("successLightest", bundle: ModuleInfo.bundle)
  }

  struct Warning {
    static let Base = Color("warningBase", bundle: ModuleInfo.bundle)
    static let Dark = Color("warningDark", bundle: ModuleInfo.bundle)
    static let Light = Color("warningLight", bundle: ModuleInfo.bundle)
    static let Lighter = Color("warningLighter", bundle: ModuleInfo.bundle)
    static let Lightest = Color("warningLightest", bundle: ModuleInfo.bundle)
  }
}
